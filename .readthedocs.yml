version: 2

sphinx:
  configuration: doc/conf.py

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs

build:
  os: ubuntu-24.04
  tools:
    python: "3.11"
  apt_packages:
    - graphviz
